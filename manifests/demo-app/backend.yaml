apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: cilium-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: kennethreitz/httpbin:latest
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: cilium-demo
spec:
  selector:
    app: backend
  ports:
  - port: 80
    targetPort: 80
